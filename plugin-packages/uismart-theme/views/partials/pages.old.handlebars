<ul class="topnav">
    <a href="#home" class="active">Home</a>
    <a href="#news">News</a>
    <a href="#contact">Contact</a>
    <div class="dropdown">
        <button class="dropbtn">Dropdown
            <i class="fa fa-caret-down"></i>
        </button>
        <div class="dropdown-content">
            <a href="#">Link 1</a>
            <a href="#">Link 2</a>
            <a href="#">Link 3</a>
        </div>
    </div>
    <a href="#about">About</a>
    <a href="javascript:void(0);" class="icon" onclick="myFunction()">&#9776;</a>
    {{!-- {{#each this}}
    {{#unless hidden}}
    <li class="nav-item">
        {{#equals pageId @root.page.pageId}}
        {{#ifSpaMode}}
        <a class="nav-link active" href="{{@root.siteBasePath}}{{friendlyUrl}}"
            onclick="return replacePageContent('{{pageId}}', '{{@root.siteBasePath}}{{friendlyUrl}}')"
            data-mr-page-id="{{pageId}}">{{title}}</a>
        {{else}}
        <a class="nav-link active" href="" onclick="return false">{{title}}</a>
        {{/ifSpaMode}}
        {{else}}
        {{#ifSpaMode}}
        <a class="nav-link" href="{{@root.siteBasePath}}{{friendlyUrl}}"
            onclick="return replacePageContent('{{pageId}}', '{{@root.siteBasePath}}{{friendlyUrl}}')"
            data-mr-page-id="{{pageId}}">{{title}}</a>
        {{else}}
        <a class="nav-link" href="{{@root.siteBasePath}}{{friendlyUrl}}">{{title}}</a>
        {{/ifSpaMode}}
        {{/equals}}
        {{#if subPages}}
        {{> pages subPages}}
        {{/if}}
    </li>
    {{/unless}}
    {{/each}} --}}
</ul>
